import tkinter as tk
from tkinter import messagebox

root = tk.Tk()

def show_msg():
    messagebox.showinfo("Info", "Button Clicked")

btn = tk.Button(root, text="Show Message", command=show_msg)
btn.pack()

root.mainloop()
